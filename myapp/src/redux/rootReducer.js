const initialValue = {
	users: [],
	members: [],
	movies: [],
	subscriptions: [],
}

const cinemaChanger = (state = initialValue, action) => {
	switch (action.type) {
		case "LOAD_USERS": {
			break
		}
		case "LOAD_MOVIES": {
			break
		}
		case "LOAD_MEMBERS": {
			break
		}
		case "LOAD_SUBSCRIPTIONS": {
			break
		}
	}
}

export default cinemaChanger
